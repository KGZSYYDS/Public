#as调用后可以给库函数起别名
import turtle as t

#设置画布属性：长=800，宽=600，底色=“红色”
t.screensize(800,600,"red")

#turtle同样是左上角为原点展开坐标轴，同cv，但是可以通过以下方式修改坐标轴
#setup第一个参数是画布长度，第二个参数是画布宽度，也可以修改画布属性，第三个、第四个参数分别为设定的原点坐标
t.setup(800,600,400,300)
t.done()

